<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.balda.listtest.MainActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/ic_title_banner"
        android:id="@+id/imageViewTitleBanner"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:scaleType="centerCrop"
        android:adjustViewBounds="false"
        android:cropToPadding="false" />

    <Button
        android:layout_width="match_parent"
        android:id="@+id/buttonShowBreweriesList"
        android:text="@string/breweries"
        android:textColor="@color/colorAccent"
        android:fontFamily="sans-serif-medium"
        android:layout_below="@+id/imageViewTitleBanner"
        android:layout_alignParentStart="true"
        android:layout_marginTop="32dp"
        android:layout_height="100dp"
        android:onClick="onClickBreweries" />

    <Button
        android:layout_width="match_parent"
        android:id="@+id/buttonShowBeersList"
        android:text="@string/beers"
        android:textColor="@color/colorAccent"
        android:fontFamily="sans-serif-medium"
        android:layout_height="100dp"
        android:layout_below="@+id/buttonShowBreweriesList"
        android:layout_alignParentStart="true" />
</RelativeLayout>
